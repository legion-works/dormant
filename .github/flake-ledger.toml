# Open-incident signatures are deterministic placeholders until a classifier records a real failure signature; prefix bare classifier output with sha256:.
schema_version = 1

[[incident]]
id = "FLAKE-2026-08-05-VCP-COMMAND-PRIORITY"
test = "vcp_ops::tests::command_priority_serializes_two_concurrent_transactions"
first_seen = "2026-08-05"
signature = "sha256:37b6ea4ad3dfa105a37cd62771b9148262549ea9d2f241a6af378577705ddb2c"
first_run_url = "https://github.com/legion-works/dormant/actions/runs/31033439943"
last_run_url = "https://github.com/legion-works/dormant/actions/runs/31162098104"
mechanism = "real-clock threshold under CPU contention on shared CI runners: t1 is expected to acquire the shared panel lock and enter a 60ms fake VCP delay before t2 arrives after a 15ms scheduler sleep, so t2 should wait at least the remaining 40ms; scheduler latency can delay t1's lock acquisition or distort the sleep and leave t2 below that bound even though the lock implementation is correct. The companion 60ms combined wall-clock assertion did NOT capture serialization as originally claimed: t1 sleeps 60ms whether or not the two overlap, so concurrent and serialized runs both satisfy it, leaving the flaky 40ms bound as the only discriminating assertion."
reproduction = "cargo nextest run -p dormant-displays --lib --stress-count 100 --retries 0 --flaky-result fail -E 'test(command_priority_serializes_two_concurrent_transactions)'"
root_cause = "The assertion measured the scheduler, not the lock. A 15ms stagger into a 60ms delay left roughly 5ms of margin before the 40ms bound; on a shared macOS runner the stagger and t1's lock acquisition drift enough to close it (observed 38.365208ms in run 31135976767, and again in 31162098104). The panel lock was correct in both failures."
fix_pr = "https://github.com/legion-works/dormant/pull/261"
proving_test = "vcp_ops::tests::command_priority_serializes_two_concurrent_transactions"
soak_evidence = [
  "https://github.com/legion-works/dormant/actions/runs/31309125912",
]
status = "fixed"
# The classifier signature is not a usable recurrence key for this incident:
# it hashes the panic message, which embeds the measured elapsed time, so every
# occurrence produced a different hash (37b6ea4a..., c20ede0b...).
# Recurrence was identified by test name and mechanism instead.

[[incident]]
id = "FLAKE-0009"
test = "daemon_smoke::manual_only_display_full_lifecycle_across_reload"
first_seen = "2026-07-19"
signature = "sha256:af2735473577639aa28799efd8f8640f00d6b21f8747b134ac81fa4f0b317bb5"
first_run_url = ""
last_run_url = ""
mechanism = "cross-generation-lost-command-window-during-reload-teardown; canonical lifecycle anchor, while FLAKE-0094 uses its distinct phase-race symptom test"
reproduction = "cargo nextest run -p dormantd --test daemon_smoke --stress-count 100 --retries 0 --flaky-result fail -E 'test(manual_only_display_full_lifecycle_across_reload)'"
root_cause = ""
triage_deferred_until = "2026-09-30"
triage_deferred_reason = "Needs a reload-teardown instrumentation pass, not a test tweak: the symptom is a command issued against generation N landing after N has been torn down, so diagnosing it requires per-generation command tracing through App::reload that does not exist yet. Shares a mechanism with FLAKE-0094 and should be investigated with it -- fixing either in isolation risks moving the race rather than closing it."
fix_pr = ""
proving_test = ""
soak_evidence = []
status = "open"

[[incident]]
id = "FLAKE-0047"
test = "boot_rollback::bad_config_with_lkg_immediate_rollback"
first_seen = "2026-07-19"
signature = "sha256:f184e0efb5599a8d44ec19f312cd6c967a0e6a0698874d1306739398dc8c3106"
first_run_url = ""
last_run_url = ""
mechanism = "shared-global-subscriber-buffer-contention-across-boot-tests"
reproduction = "cargo nextest run -p dormantd --test boot_rollback --stress-count 100 --retries 0 --flaky-result fail -E 'test(bad_config_with_lkg_immediate_rollback)'"
root_cause = ""
triage_deferred_until = "2026-09-30"
triage_deferred_reason = "Root cause is suspected to be the process-global tracing subscriber buffer shared across boot_rollback tests, which means the fix is a test-harness change (per-test subscriber isolation) affecting every test that asserts on captured logs. That is a wide blast radius for a 1-in-N flake and wants its own change, not a rushed one alongside a release."
fix_pr = ""
proving_test = ""
soak_evidence = []
status = "open"

[[incident]]
id = "FLAKE-0070"
test = "audio_policy::tests::timing_policy_cases_are_deterministic"
first_seen = "2026-07-19"
signature = "sha256:967e52e38eb53014530ff660a1665a8c37977d3ed46cbfe166e7107b6f38da07"
first_run_url = ""
last_run_url = ""
mechanism = "real-clock-timing-window-on-a-loaded-runner; the original narrow audio_source poller assertion was replaced by this deterministic audio_policy coverage"
reproduction = "cargo nextest run -p dormantd --lib --stress-count 100 --retries 0 --flaky-result fail -E 'test(timing_policy_cases_are_deterministic)'"
root_cause = "the flaky assertion no longer exists. The original narrow audio_source poller assertion depended on a real-clock timing window on a loaded runner and was REPLACED by audio_policy::tests::timing_policy_cases_are_deterministic, which drives table-driven cases through pure decision logic. Verified: zero thread::sleep / tokio sleep / elapsed() calls in audio_policy.rs. The incident stayed open because the replacement landed without the ledger being closed out."
fix_pr = ""
proving_test = "audio_policy::tests::timing_policy_cases_are_deterministic passes under full CPU saturation (16 spinners on 16 cores) in 0.003s -- a real-clock dependency could not complete in that time under that load"
soak_evidence = []
status = "open"

[[incident]]
id = "FLAKE-0080"
test = "daemon_smoke::wear_shutdown_persists_final_ledger"
first_seen = "2026-07-19"
signature = "sha256:94f35ab1c8c0965a66b098bee4e51f0105cb38e287fb84b646e7b941eecfa51d"
first_run_url = ""
last_run_url = ""
mechanism = "persist-read-race-left-trailing-characters-in-the-wear-ledger"
reproduction = "cargo nextest run -p dormantd --test daemon_smoke --stress-count 100 --retries 0 --flaky-result fail -E 'test(wear_shutdown_persists_final_ledger)'"
root_cause = ""
triage_deferred_until = "2026-09-30"
triage_deferred_reason = "Trailing characters in the wear ledger point at a read racing a non-atomic persist. The persist path already writes atomically via rename, so either the test reads outside that guarantee or a second writer exists -- distinguishing those requires instrumenting wear_tracker's persist path with write-generation markers. Until that distinction is made, any fix would be a guess about which of two mechanisms is real."
fix_pr = ""
proving_test = ""
soak_evidence = []
status = "open"

[[incident]]
id = "FLAKE-0086"
test = "watchdog_schedule::tests::healthy_ticks_keep_a_fixed_cadence"
first_seen = "2026-07-19"
signature = "sha256:270ba6942a1967bf4792bc613c01b1d3c92644f312aaf9aa2a1927db6f76209f"
first_run_url = ""
last_run_url = ""
mechanism = "real-cadence-sleep-flake; the original daemon_smoke watchdog_ping_cadence_grows_on_healthy_ticks test was deleted and this schedule test is its replacement coverage"
reproduction = "cargo nextest run -p dormantd --lib --stress-count 100 --retries 0 --flaky-result fail -E 'test(healthy_ticks_keep_a_fixed_cadence)'"
root_cause = "the flaky assertion no longer exists. The original test (daemon_smoke::watchdog_ping_cadence_grows_on_healthy_ticks) measured ping cadence against the real clock on a shared runner and was DELETED, not repaired; watchdog_schedule::tests::healthy_ticks_keep_a_fixed_cadence is its replacement and computes deadlines arithmetically from a single Instant with no sleeping. Verified: zero thread::sleep / tokio sleep / elapsed() calls in watchdog_schedule.rs, and the deleted test name has no occurrences anywhere in crates/. The incident stayed open because the replacement landed without the ledger being closed out."
fix_pr = ""
proving_test = "watchdog_schedule::tests::healthy_ticks_keep_a_fixed_cadence passes under full CPU saturation (16 spinners on 16 cores) in 0.003s -- a real-clock dependency could not complete in that time under that load"
soak_evidence = []
status = "open"

[[incident]]
id = "FLAKE-0092"
test = "daemon_smoke::config_watch_updates_on_successful_reload_only"
first_seen = "2026-07-19"
signature = "sha256:528ebd736514efab11359cf2a267153e2d3f712fe78e600168bef75be170640c"
first_run_url = ""
last_run_url = ""
mechanism = "watcher-trigger-and-explicit-trigger-reloaded-the-same-content-twice"
reproduction = "cargo nextest run -p dormantd --test daemon_smoke --stress-count 100 --retries 0 --flaky-result fail -E 'test(config_watch_updates_on_successful_reload_only)'"
root_cause = ""
triage_deferred_until = "2026-09-30"
triage_deferred_reason = "The watcher and the explicit trigger reload identical content, so the assertion cannot currently tell 'reloaded twice' from 'reloaded once and observed twice'. Fixing it means giving reload observations a causal identity (revision + origin) rather than tightening a count -- a dormant-core change, not a test change."
fix_pr = ""
proving_test = ""
soak_evidence = []
status = "open"

[[incident]]
id = "FLAKE-0094"
test = "daemon_smoke::manual_only_display_no_defensive_wake_on_reload"
first_seen = "2026-07-19"
signature = "sha256:c89341cc0f4d657ec05d3b06c804309a07f2785f1f2d3edd469297190f79d845"
first_run_url = ""
last_run_url = ""
mechanism = "marker-write-precedes-phase-landing-and-can-preserve-blanking; distinct symptom anchor resolves the historical FLAKE-0009 same-test collision"
reproduction = "cargo nextest run -p dormantd --test daemon_smoke --stress-count 100 --retries 0 --flaky-result fail -E 'test(manual_only_display_no_defensive_wake_on_reload)'"
root_cause = ""
triage_deferred_until = "2026-09-30"
triage_deferred_reason = "The marker write can precede the phase landing, so the test observes an intermediate state that is legal but not the one asserted. Needs the same per-generation reload tracing as FLAKE-0009 and should be taken with it; the two were split precisely because they are distinct symptoms of one underlying window."
fix_pr = ""
proving_test = ""
soak_evidence = []
status = "open"

[[incident]]
id = "FLAKE-2026-07-18-WEAR-CORRUPT"
test = "wear_tracker::tests::corrupt_rename_failure_logs_wear_ledger_corrupt_not_persist_failed"
first_seen = "2026-07-19"
signature = "sha256:28fc1067a28ff77488f407641ff792f872c9e97ded82592ea5fff9c0e0ddaf64"
first_run_url = ""
last_run_url = ""
mechanism = "capture-plane-global-state-dependent-observation"
reproduction = "cargo nextest run -p dormantd --lib --stress-count 100 --retries 0 --flaky-result fail -E 'test(corrupt_rename_failure_logs_wear_ledger_corrupt_not_persist_failed)'"
root_cause = ""
triage_deferred_until = "2026-09-30"
triage_deferred_reason = "Observation depends on process-global capture-plane state, so the test can see another test's log lines under nextest parallelism. Same class as FLAKE-0047 (shared subscriber state) and likely closed by the same per-test isolation work; deferring to be fixed together rather than patched twice in different ways."
fix_pr = ""
proving_test = ""
soak_evidence = []
status = "open"

[[incident]]
id = "FLAKE-2026-07-18-MPV-FIRSTFRAME"
test = "screensaver::tests::mpv_render_context_created_with_advanced_control"
first_seen = "2026-07-19"
signature = "sha256:6c3c65304df27ffa40bf0088783c56e9adc175e97011ff2bbec3b3f7648ace3b"
first_run_url = ""
last_run_url = ""
mechanism = "real-clock first-frame deadline in the libmpv render-context test; fails intermittently under full-workspace CPU contention, passes when run isolated (3 sightings during M1 T5/T6/T7 full-suite runs; re-observed by the ci profile during M3 T17)"
reproduction = "cargo nextest run --profile ci --workspace --all-features"
root_cause = "real-clock frame-production deadlines in the mpv render tests were PERFORMANCE gates, not hang detectors. mpv decodes on its own threads; under nextest parallelism those threads get starved for far longer than the decode itself takes, so any bound tight enough to assert speed fails on LOAD rather than on regression. This test allowed 10s for 5 frames; the sibling MPV-FIRSTFRAME allowed 200ms total (10 polls x 20ms) for mpv to initialise AND decode its first frame. Three further sites in the same file carried the same shape unreported (8s, 5s, and a 20x20ms loop). All five now share FRAME_PUMP_TIMEOUT = 60s, documented as a hang detector. Raising it costs nothing in the passing case: every loop exits as soon as it has the frames it needs, so the bound only binds on a real failure."
fix_pr = ""
proving_test = "mutation control: FRAME_PUMP_TIMEOUT = 1ms makes both ledger tests fail (exit 100, both named); at 60s both pass under full CPU saturation (16 spinners on a 16-core box)"
soak_evidence = []
status = "open"

[[incident]]
id = "FLAKE-2026-07-18-RENDER-FRAMES"
test = "screensaver::tests::renders_non_zero_changing_frames"
first_seen = "2026-07-19"
signature = "sha256:5ad1de95466bf0815d42052c0c74653b176e3015c1c0cde9ef884e824b30ef14"
first_run_url = ""
last_run_url = ""
mechanism = "real-clock libmpv frame-production deadline in the screensaver render tests; sibling of FLAKE-2026-07-18-MPV-FIRSTFRAME — same mechanism, different test; retries-to-pass under full-workspace CPU contention, observed by the ci profile during M3 T18"
reproduction = "cargo nextest run -p dormant-render --lib --stress-count 100 --retries 0 --flaky-result fail -E 'test(renders_non_zero_changing_frames)'"
root_cause = "real-clock frame-production deadlines in the mpv render tests were PERFORMANCE gates, not hang detectors. mpv decodes on its own threads; under nextest parallelism those threads get starved for far longer than the decode itself takes, so any bound tight enough to assert speed fails on LOAD rather than on regression. This test allowed 10s for 5 frames; the sibling MPV-FIRSTFRAME allowed 200ms total (10 polls x 20ms) for mpv to initialise AND decode its first frame. Three further sites in the same file carried the same shape unreported (8s, 5s, and a 20x20ms loop). All five now share FRAME_PUMP_TIMEOUT = 60s, documented as a hang detector. Raising it costs nothing in the passing case: every loop exits as soon as it has the frames it needs, so the bound only binds on a real failure."
fix_pr = ""
proving_test = "mutation control: FRAME_PUMP_TIMEOUT = 1ms makes both ledger tests fail (exit 100, both named); at 60s both pass under full CPU saturation (16 spinners on a 16-core box)"
soak_evidence = []
status = "open"

[[incident]]
id = "FLAKE-2026-07-23-TRAY-IPC-EVENT-ORDER"
test = "ipc_roundtrip::ipc_status_and_events_roundtrip_via_dormantctl_client"
first_seen = "2026-07-23"
signature = "sha256:f2f4bd460f4e28af11b53d150070cd8c14b4896aeeeac2e8c0b3a10527775793"
first_run_url = "https://github.com/legion-works/dormant/actions/runs/30028323908"
last_run_url = "https://github.com/legion-works/dormant/actions/runs/30032324404"
mechanism = "event-order assumption vs wear-tracker emission race; the test asserted the FIRST event frame after subscribing was DisplayPhase, but the wear tracker's first tick fires at startup and emits a WearSnapshot for every display (each display's persist clock starts at zero, so the persist-cadence check trips on tick one) and SensorChanged/ZoneChanged from the fake sensor's startup sample can also land first under load — only event ARRIVAL is guaranteed; fixed by draining non-target frames (bounded deadline, no sleeps, mirroring recv_terminal_outcome in daemon_smoke.rs and the spawn_event_pump reader in ipc_loop.rs) until DisplayPhase arrives, with an honest timeout panic listing every observed frame"
reproduction = "cargo nextest run -p dormant-tray --test ipc_roundtrip --stress-count 100 --retries 0 --flaky-result fail -E 'test(ipc_status_and_events_roundtrip_via_dormantctl_client)'"
root_cause = "the test assumed the first subscribed event was DisplayPhase, but the wear tracker emits an immediate WearSnapshot and startup sensor/zone events can arrive first under load"
fix_pr = "https://github.com/legion-works/dormant/pull/130"
proving_test = "ipc_roundtrip::ipc_status_and_events_roundtrip_via_dormantctl_client"
soak_evidence = [
  "https://github.com/legion-works/dormant/actions/runs/30032324404",
]
status = "fixed"

[[incident]]
id = "FLAKE-2026-07-25-CLAIM-LISTENER-PORT-RACE"
test = "coordination_claim::tests::ensure_and_release_provisional_obey_empty_peer_snapshot"
first_seen = "2026-07-26"
signature = "sha256:d5e74e97c5d35e1791e87caa5af2727c7fd44008aa2fe62c718849d12ab736d9"
first_run_url = ""
last_run_url = ""
mechanism = "shared-ephemeral-port-race-under-parallel-load: three tests in coordination_claim released a TCP listener and then asserted TcpStream::connect(old_port).is_err() — under parallel load another test or process can bind the just-released ephemeral port during the window, making connect succeed and the assertion fail. The assertion intent ('my listener is gone') was already covered by the supervisor state check (provisional_port() == None, or provisional_port() != old_port) on the preceding line."
reproduction = "cargo test -p dormant-core -p dormantd -p dormant-doctor --all-features (parallel) — failures at different test sites each run"
root_cause = "tests probed OS kernel port-ownership rather than supervisor state; ephemeral port reuse is a kernel-level race that the test cannot prevent"
fix_pr = ""
proving_test = "coordination_claim::tests::parked_when_empty_and_binds_on_first_peer"
soak_evidence = []
status = "obsolete"

[[incident]]
id = "FLAKE-2026-07-26-COORDINATOR-BATCH-WATCHER-RACE"
test = "daemon_smoke::coordinator_batches_watcher_and_web_apply_for_one_revision"
first_seen = "2026-07-26"
signature = "sha256:1ef0f7a8190aac2fae141b7c70e2ce0df3cdac01c7edf8bb9f3eadfbd026f161"
first_run_url = "https://github.com/legion-works/dormant/actions/runs/30206030529"
last_run_url = "https://github.com/legion-works/dormant/actions/runs/30206030529"
mechanism = "real `notify` filesystem watcher injected a third, uncontrolled ReloadRequest into the batch coordinator: a `fs::write` fires the real watcher, which under CI load completes its batch and advances `applied_revision` before the test's two explicit `request_reload_with_id` calls are scheduled — they then form a second batch where `requested_revision == applied_revision`, triggering the coalesced no-op path (`coalesced: true`) and failing the `!coalesced` assertion. The sibling test `coordinator_batches_watcher_and_control_for_one_revision` has the identical shape and racelands."
reproduction = "cargo nextest run -p dormantd --test daemon_smoke --config-file .config/nextest.toml -P ci -E 'test(/coordinator_batches_/)' --retries 0"
root_cause = "the batch coordinator tests wrote config files via `fs::write` (which fires the real `notify` file watcher) and raced two explicit reload requests against the uncontrolled watcher — the watcher's batch could complete under CI load before the explicit requests were enqueued"
fix_pr = ""
proving_test = "daemon_smoke::coordinator_config_watcher_suppression_is_effective"
soak_evidence = []
status = "open"
