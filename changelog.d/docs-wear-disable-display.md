---
kind: fix
surfaces: [docs]
issues: []
---
Detail: The active-wear-sampling and troubleshooting pages now show `dormantctl wear disable-sampling` with the `--display` flag. The bare form they previously showed fails on any configuration with more than one sampled display, which includes the reference setup.
