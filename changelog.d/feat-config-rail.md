---
kind: capability
surfaces: [readme]
readme_bullet: "**Config section rail** — sticky per-tab section navigation with scroll-spy in the web UI"
---
User can now: jump straight to any section of a config tab from a sticky
side rail — the rail tracks scroll position, highlights the active section,
and section links survive as shareable URL hashes without breaking existing
field deep links.
