---
kind: fix
surfaces: []
issues: [310]
---
Detail: The direct-hardware fallback of `dormantctl emergency-wake` now bounds each per-display wake, so a wedged DDC bus is reported as a failed display instead of hanging the command before it prints its report and restores gamma.
