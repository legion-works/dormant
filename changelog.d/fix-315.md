---
kind: fix
surfaces: []
issues: [315]
---
Detail: Samsung pairing tokens are now written under a lock that spans the whole read-modify-write, with a unique temporary filename per write, so two writers can no longer lose one another's token and force a re-pairing. The temporary file is also created owner-only from the outset rather than tightened afterwards, and is removed if the write fails.
