---
kind: fix
surfaces: []
issues: [316]
---
Detail: The screensaver's buffers are now rebuilt when a live output resize changes the display geometry, so the overlay no longer keeps drawing at the pre-resize size. The rebuild also draws the current frame before committing and cancels the pending item-load deadline, so playback neither blacks out nor stalls afterwards.
