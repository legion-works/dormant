---
kind: fix
surfaces: []
issues: [317]
---
Detail: The CLI now bounds every IPC response read, with per-request bounds derived from the daemon: five minutes for the screen-capture consent prompt, longer allowances for display switching and sampling teardown, and two seconds for the rest. A daemon that accepts a connection and then stalls no longer hangs the CLI.
