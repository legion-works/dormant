---
kind: fix
surfaces: []
issues: [319]
---
Detail: Tray actions targeting a single display now check that the display still exists before sending a request, so a click on a menu item left stale by a reload or hotplug is ignored instead of naming a display the daemon no longer has.
