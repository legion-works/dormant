/**
 * Config view — loaded config display and validation.
 *
 * Stub for Task 8.  Task 12 fills this in with the TOML viewer,
 * parsed inventory, and validation card.
 */
export default function Config() {
  return (
    <div style={{ padding: "var(--space-6)" }}>
      <h2 style={{ fontFamily: "var(--font-display)", color: "var(--text-strong)" }}>
        Config
      </h2>
      <p style={{ color: "var(--text-muted)", fontFamily: "var(--font-mono)", fontSize: "12px" }}>
        Task 12 — full implementation
      </p>
    </div>
  );
}
