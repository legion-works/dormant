/**
 * Dashboard view — at-a-glance health of the whole pipeline.
 *
 * Stub for Task 8.  Task 9 fills this in with the stat row,
 * signal-flow columns, and recent-activity list.
 */
export default function Dashboard() {
  return (
    <div style={{ padding: "var(--space-6)" }}>
      <h2 style={{ fontFamily: "var(--font-display)", color: "var(--text-strong)" }}>
        Dashboard
      </h2>
      <p style={{ color: "var(--text-muted)", fontFamily: "var(--font-mono)", fontSize: "12px" }}>
        Task 9 — full implementation
      </p>
    </div>
  );
}
