/**
 * Doctor view — environment & integration diagnostics.
 *
 * Stub for Task 8.  Task 13 fills this in with the summary row
 * and check list.
 */
export default function Doctor() {
  return (
    <div style={{ padding: "var(--space-6)" }}>
      <h2 style={{ fontFamily: "var(--font-display)", color: "var(--text-strong)" }}>
        Doctor
      </h2>
      <p style={{ color: "var(--text-muted)", fontFamily: "var(--font-mono)", fontSize: "12px" }}>
        Task 13 — full implementation
      </p>
    </div>
  );
}
