/**
 * Events view — live daemon event stream (WS-backed).
 *
 * Stub for Task 8.  Task 11 fills this in with the scrolling
 * event log, type badges, and WS integration.
 */
export default function Events() {
  return (
    <div style={{ padding: "var(--space-6)" }}>
      <h2 style={{ fontFamily: "var(--font-display)", color: "var(--text-strong)" }}>
        Events
      </h2>
      <p style={{ color: "var(--text-muted)", fontFamily: "var(--font-mono)", fontSize: "12px" }}>
        Task 11 — full implementation
      </p>
    </div>
  );
}
